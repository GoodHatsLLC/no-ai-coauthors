name: no-ai-coauthors

on:
  pull_request:
  push:

jobs:
  no-ai-coauthors:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: GoodHatsLLC/no-ai-coauthors@<commit-sha-or-tag>
